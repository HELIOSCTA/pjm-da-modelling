name: helioscta-power-pjm

prefect-version: 3.0.0

build: null
push: null

pull:
  - prefect.deployments.steps.set_working_directory:
      directory: /app

deployments:

  # ────── PJM Transmission Outages Daily ──────
  # Daily flow: scrape eDART linesout.txt, capture SCD2 snapshot of the source,
  # then build the four view marts (active / 7-day window / 24h changes simple +
  # snapshot variants).
  #
  # Upstream cadence: PJM publishes linesout.txt continuously throughout the
  # day; scheduling once daily is enough for morning brief consumption.
  # We run at 05:00 MST to land before the morning fundies prompt; the 06:00
  # and 07:00 retries give two hours of recovery if the eDART download fails.

  - name: PJM Transmission Outages Daily
    entrypoint: schedulers/prefect/power/pjm/pjm_transmission_outages_daily_flows.py:pjm_transmission_outages_daily
    work_pool:
      name: default-agent-pool
      work_queue_name: default
    tags: [pjm, power, transmission_outages_daily]
    schedules:

      # ────── Daily at 05:00 MST ──────
      - cron: "0 5,6,7 * * *"
        timezone: "America/Denver"
